package ;
import {APPCLASS};

class {MAINBUILDCLASS} {

    static public function main():Void {
        if (js.Browser.document.readyState == 'complete') new {APPCLASS}();
        else js.Browser.window.addEventListener('load', () -> {
            new {APPCLASS}();
        });
    }
}