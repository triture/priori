package ;
import {APPCLASS};

class {MAINBUILDCLASS} {

    static public function main():Void {
        js.Browser.window.addEventListener('load', () -> {
            new {APPCLASS}();
        });
    }
}