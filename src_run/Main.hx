package ;

import helper.Helper;

class Main {

    public static function main() {

        var run:PrioriRun = new PrioriRun(Sys.args());

        Helper.g().output.print("");
        Helper.g().output.print("\n");

        Sys.exit(0);
    }
}
